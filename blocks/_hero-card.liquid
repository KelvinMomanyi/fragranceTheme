<li class="{% if block.settings.is_large %}colspan-2{% endif %}" {{ block.shopify_attributes }}>
  <div class="hero-card">
    <div class="gradient-overlay">
      <figure class="card-banner img-holder" style="--width: {{ block.settings.width | default: 285 }}; --height: {{ block.settings.height | default: 396 }};">
        {% if block.settings.image != blank %}
          <img src="{{ block.settings.image | image_url: width: 1200 }}"
               srcset="{{ block.settings.image | image_url: width: 285 }} 285w,
                       {{ block.settings.image | image_url: width: 570 }} 570w,
                       {{ block.settings.image | image_url: width: 600 }} 600w,
                       {{ block.settings.image | image_url: width: 900 }} 900w,
                       {{ block.settings.image | image_url: width: 1200 }} 1200w"
               sizes="(max-width: 575px) 100vw, {% if block.settings.is_large %}568px{% else %}285px{% endif %}"
               width="{{ block.settings.width | default: 285 }}" height="{{ block.settings.height | default: 396 }}" 
                alt="{{ block.settings.image.alt | escape }}" class="img-cover"
               style="{% render 'focal-point-style', image: block.settings.image %}"
               fetchpriority="high">
        {% else %}
          {{ block.settings.svg_placeholder | default: 'product-1' | placeholder_svg_tag: 'img-cover' }}
        {% endif %}
      </figure>
    </div>
    <div class="card-content">
      {% content_for 'blocks' %}
    </div>
    {% if block.settings.link != blank %}
      <a href="{{ block.settings.link }}" class="full-card-link" style="position: absolute; top: 0; left: 0; width: 100%; height: 100%; z-index: 1;" aria-label="View product"></a>
    {% endif %}
  </div>
</li>

{% schema %}
{
  "name": "Hero Card",
  "tag": null,
  "blocks": [
    {
      "type": "_hero-title"
    },
    {
      "type": "_hero-button"
    },
    {
      "type": "@theme"
    },
    {
      "type": "@app"
    },
    {
       "type": "group"
    }
  ],
  "settings": [
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "checkbox",
      "id": "is_large",
      "label": "Large Card (2 Columns)",
      "default": false
    },
    {
      "type": "number",
      "id": "width",
      "label": "Aspect Ratio Width",
      "default": 285
    },
    {
      "type": "number",
      "id": "height",
      "label": "Aspect Ratio Height",
      "default": 396
    },
    {
      "type": "select",
      "id": "svg_placeholder",
      "label": "Placeholder SVG",
      "options": [
        { "value": "product-1", "label": "Product 1" },
        { "value": "product-2", "label": "Product 2" },
        { "value": "product-3", "label": "Product 3" },
        { "value": "product-4", "label": "Product 4" },
        { "value": "product-5", "label": "Product 5" },
        { "value": "product-6", "label": "Product 6" }
      ],
      "default": "product-1"
    },
    {
      "type": "url",
      "id": "link",
      "label": "Card Link (Optional)"
    }
  ],
  "presets": [
    {
      "name": "Hero Card",
      "blocks": [
        {
          "type": "_hero-title"
        },
        {
          "type": "_hero-button"
        }
      ]
    }
  ]
}
{% endschema %}
